import React from 'react';
import ReactDOM from 'react-dom/client';
import LectureOverlay from '../components/LectureOverlay';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <LectureOverlay />
  </React.StrictMode>
);
